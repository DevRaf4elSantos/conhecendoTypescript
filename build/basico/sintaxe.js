// Objetos e Funções: 
function criarObjeto(m, mc, km, placa) {
    // Crio a tipo (estrutura) do objeto.
    let carro;
    carro = { modelo: m, marcar: mc, placa: placa, quilometragem: km };
    console.log(carro);
}
export {};
